var lista = [5, 2, 9, 1, 5, 6];

functie bubbleSort(var l) {
    var n = lungime(l);
    pentru (var i = 0; i < n - 1; i = i + 1) executa {
        pentru (var j = 0; j < n - i - 1; j = j + 1) executa {
            daca (l[j] > l[j + 1]) atunci {
                var temp = l[j];
                l[j] = l[j + 1];
                l[j + 1] = temp;
            }
        }
    }
    returneaza l;
}

var sortata = bubbleSort(lista);
afiseaza(sortata);
