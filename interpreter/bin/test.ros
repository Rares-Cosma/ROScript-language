importa "vector" ca v;

functie calc_val_corecte(var x_test){
    var val_corecte=[];
    pentru (var i=0;i<lungime(x_test);i++){
        v.adauga(val_corecte,3*x_test[i,0]-2*x_test[i,1]+0.5*x_test[i,2]+x_test[i,3]-x_test[i,4]+7);
    }
    returneaza val_corecte;
}